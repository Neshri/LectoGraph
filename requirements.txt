# --- Primary PyTorch Index ---
# We use --extra-index-url so pip can still find tiktoken/numpy/etc on PyPI
--extra-index-url https://download.pytorch.org/whl/cu124

# --- Core AI and ML Dependencies ---
# The +cu124 suffix forces pip to get these from the PyTorch repo
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
tiktoken
ctranslate2>=4.0.0
faster-whisper>=1.0.0
openai-whisper>=20240930
transformers>=4.35.0

# --- Other Application Dependencies ---
ffmpeg-python>=0.2.0
markdown
msal>=1.26.0
mutagen>=1.46.0
numpy>=1.21.0
python-dotenv>=0.19.0
PyJWT>=2.0.0
requests>=2.25.0
lightrag-hku
openscenesense-ollama
pyyaml>=6.0